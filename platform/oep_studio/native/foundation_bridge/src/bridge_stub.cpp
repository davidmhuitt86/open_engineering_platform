// oep_foundation_bridge.dll has no source of its own — it exists only
// to link oep_api's static library into a shared library so dart:ffi
// has something to open. This translation unit is intentionally empty;
// see ../CMakeLists.txt for why it's needed at all.
