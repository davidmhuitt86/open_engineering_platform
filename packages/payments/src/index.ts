/**
 * Commerce: purchases, subscriptions, and revenue distribution (EXC-010).
 *
 * Status: Excluded from WP-EXC-001's scope. Explicitly excluded from WP-EXC-001 ("Commerce", "Revenue Distribution" — see the task doc's own Scope §5 Excluded list). Intentionally empty beyond this scaffold.
 */
export const PACKAGE_NAME = '@oep-exchange/payments' as const;
