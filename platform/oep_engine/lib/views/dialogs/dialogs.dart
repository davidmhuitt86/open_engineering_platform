/// Generic, host-agnostic drafting dialogs (WORK_PACKAGE_024) — promoted
/// from the Demonstration Host for the same reason as `views/widgets/`:
/// plain Material + Engine data types only, so Diagram Studio reuses
/// them instead of duplicating ~250 lines of dialog code.
library;

export 'array_placement_dialog.dart';
export 'grid_settings_dialog.dart';
export 'named_layouts_dialog.dart';
