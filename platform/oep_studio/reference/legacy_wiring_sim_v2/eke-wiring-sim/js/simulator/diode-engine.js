/**
 * simulator/diode-engine.js
 *
 * Diode test mode simulation and rectifier analysis.
 *
 * Phase 3+ placeholder.
 *
 * Methods (Phase 3):
 *   diodeReading(wire)              → { forward, reverse } (voltage drop)
 *   isForwardBiased(wire, keyPos)   → boolean
 *   rectifierOutput(moduleId)       → { vac, vdc } (for reg/rect modules)
 *
 * No DOM. No rendering. No UI.
 */
