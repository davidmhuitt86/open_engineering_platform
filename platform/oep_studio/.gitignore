# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Credentials — never committed. API keys belong in Windows Credential
# Manager (lib/core/security/), not in a file in this repository.
*.env

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# This project only scaffolds windows/android (see docs/TECH_STACK.md).
# `flutter pub get`/analyze/test/build still regenerate ephemeral plugin
# registrant files for every platform listed in .metadata, including
# linux/macos, which aren't real scaffolds here — ignore them rather
# than repeatedly cleaning them up before every commit.
/linux/flutter/
/macos/Flutter/ephemeral/
/macos/Flutter/GeneratedPluginRegistrant.swift
