/**
 * The Admin Console's root component.
 *
 * Status: scaffolded in TASK-EXC-0001 (Repository Structure) — proves
 * the Vite + React + TypeScript + Vitest toolchain is wired correctly.
 * The real Administration page (reviewing/approving submitted packages,
 * managing publishers) arrives in TASK-EXC-0009 (Web Interface).
 */
export function App(): JSX.Element {
  return (
    <main>
      <h1>OEP Engineering Exchange</h1>
      <p>Administration — scaffolded, not yet implemented.</p>
    </main>
  );
}
