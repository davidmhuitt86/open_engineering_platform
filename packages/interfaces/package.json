{
  "name": "@oep-exchange/interfaces",
  "version": "0.1.0",
  "private": true,
  "description": "Future cross-repository integration contracts. Documentation and (eventually) type-only contract declarations — never an implementation, never a dependency on any other workspace package.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  }
}
