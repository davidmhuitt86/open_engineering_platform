/**
 * editor/clipboard.js
 *
 * Copy, cut, and paste for modules and wires.
 *
 * Phase 3 placeholder.
 *
 * Phase 3 goal:
 *   copy(selection)  — serialise selected modules+wires to clipboard
 *   paste(position)  — deserialise, generate new ids, place at cursor
 *   cut(selection)   — copy + delete
 *
 * Clipboard data is stored in memory (not system clipboard) to avoid
 * security prompts and support pasting across panels.
 *
 * No rendering. No electrical logic.
 */
