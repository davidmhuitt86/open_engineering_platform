{
  "name": "@oep-exchange/core",
  "version": "0.1.0",
  "private": true,
  "description": "Domain primitives and shared engineering types used across every Exchange package and app. No dependency on any other workspace package.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  }
}
