/**
 * Checks installed packages for available updates.
 *
 * Status: Deferred beyond WP-EXC-001's explicit scope. Not in WP-EXC-001's explicit deliverable list — scaffolded now because the directory already existed, left otherwise empty until a future work package.
 */
export const PACKAGE_NAME = '@oep-exchange/update-service' as const;
