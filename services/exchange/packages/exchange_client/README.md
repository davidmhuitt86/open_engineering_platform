# @oep-exchange/exchange-client

A typed HTTP client SDK for the Exchange REST API, used by the installer, update service, and both web apps.

**Status:** Scaffolded in TASK-EXC-0001 (Repository Structure). Real implementation arrives in TASK-EXC-0007 (Download API).

## Dependency direction

Depends on `@oep-exchange/core`, `@oep-exchange/api-contracts`.
